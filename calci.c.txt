
# include <stdio.h>

int main() {

    char operator;
    double firstNumber,secondNumber;

    printf("Enter an operator (+, -, *,): ");
    scanf("%c", &operator);

    printf("Enter two operands: ");
    scanf("%lf %lf",&firstNumber, &secondNumber);

    switch(operator)
    {
        case '+':
            sum();					//changes done by saurabh
	    {
	    	int a ,b;
		cout<<"Enter two numbers";
		cin>>a>>b;
		cout<<"sum is "<<a+b;

	    }

        case '-':
            To be done by Rishabh

        case '*':
            To be done by Sajal

        case '/':
            To be done by Rachit
        default:
            By Shivam
    }
    
    return 0;
}
